{% extends '::base.html.twig' %}

{% block content -%}
    <h1>Punto de Interés</h1>

    <div class="table-responsive">
        <table class="table table-condensed table-hover">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ entity.id }}</td>
                </tr>
                <tr>
                    <th>Nombre</th>
                    <td>{{ entity.nombre }}</td>
                </tr>
                <tr>
                    <th>Descripcion</th>
                    <td>{{ entity.descripcion }}</td>
                </tr>
                <tr>
                    <th>Direccion</th>
                    <td>{{ entity.direccion }}</td>
                </tr>
                <tr>
                    <th>Contacto</th>
                    <td>{{ entity.contacto }}</td>
                </tr>
                <tr>
                    <th>Ciudad</th>
                    <td>{{ entity.ciudad }}</td>
                </tr>
                <tr>
                    <th>Latitud</th>
                    <td>{{ entity.latitud }}</td>
                </tr>
                <tr>
                    <th>Longitud</th>
                    <td>{{ entity.longitud }}</td>
                </tr>
            </tbody>
        </table>
    </div>

    <div role="group">
        <a href="{{ path('poi') }}" class="btn btn-default btn-md"><i class="fa fa-list"></i> Listado</a>
        <a href="{{ path('poi_edit', { 'id': entity.id }) }}" class="btn btn-primary btn-md"><i class="fa fa-edit"></i> Editar</a>
    </div>
{% endblock %}
